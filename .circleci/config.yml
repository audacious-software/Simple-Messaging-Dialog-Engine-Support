version: 2.1

orbs:
  python: circleci/python@1.2

workflows:
  build:
    jobs:
      - build-38
      - build-36
      - build-27

jobs:
  build-38:
    docker:
      - image: cimg/python:3.8
      - image: cimg/postgres:12.18-postgis
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: circle_test
          POSTGRES_PASSWORD: ""
    steps:
      - checkout
      - restore_cache:
          key: venv-3-{{ .Branch }}-{{ checksum "requirements.txt" }}
      - run:
          name: Build and validate
          command: |
            sudo apt-get update && sudo apt-get install -y gdal-bin
            python3 -m venv --copies /home/circleci/venv
            . /home/circleci/venv/bin/activate
            pip install -r requirements.txt --progress-bar off
            cd ..
            mv project simple_messaging_dialog_support
            mkdir django
            cd django
            django-admin startproject smds
            mv ../simple_messaging_dialog_support smds
            cd smds
            git clone https://github.com/audacious-software/Simple-Messaging-Django.git simple_messaging
            pip install -r simple_messaging/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Django-Dialog-Engine.git django_dialog_engine
            pip install -r django_dialog_engine/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Quicksilver-Django.git quicksilver
            pip install -r quicksilver/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Simple-Data-Export-Django.git simple_data_export
            pip install -r simple_data_export/requirements.txt --progress-bar off
            cp simple_messaging_dialog_support/circle_settings.py smds/settings.py
            cp simple_messaging_dialog_support/circle_urls.py smds/urls.py
            mkdir -p media/simple_data_export_uploads
            mkdir -p media/incoming_message_media
            mkdir -p media/outgoing_message_media
            python manage.py migrate
            python manage.py test
            cp simple_messaging_dialog_support/.pylintrc .
            pylint quicksilver
            pylint django_dialog_engine
            pylint simple_messaging
            pylint simple_messaging_dialog_support
            bandit -r .      
      - save_cache:
          key: venv-38-{{ .Branch }}-{{ checksum "/home/circleci/django/smds/simple_messaging_dialog_support/requirements.txt" }}
          paths:
            - "/home/circleci/venv"
      - store_test_results:
          path: test-results/
      - store_artifacts:
          path: test-reports/
          destination: python_app

  build-36:
    docker:
      - image: cimg/python:3.8
      - image: cimg/postgres:9.6.24-postgis
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: circle_test
          POSTGRES_PASSWORD: ""
    steps:
      - checkout
      - restore_cache:
          key: venv-3-{{ .Branch }}-{{ checksum "requirements.txt" }}
      - run:
          name: Build and validate
          command: |
            sudo apt-get update && sudo apt-get install -y gdal-bin
            python3 -m venv --copies /home/circleci/venv
            . /home/circleci/venv/bin/activate
            pip install -r requirements.txt --progress-bar off
            cd ..
            mv project simple_messaging_dialog_support
            mkdir django
            cd django
            django-admin.py startproject smds
            mv ../simple_messaging_dialog_support smds
            cd smds
            git clone https://github.com/audacious-software/Simple-Messaging-Django.git simple_messaging
            pip install -r simple_messaging/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Django-Dialog-Engine.git django_dialog_engine
            pip install -r django_dialog_engine/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Quicksilver-Django.git quicksilver
            pip install -r quicksilver/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Simple-Data-Export-Django.git simple_data_export
            pip install -r simple_data_export/requirements.txt --progress-bar off
            cp simple_messaging_dialog_support/circle_settings.py smds/settings.py
            cp simple_messaging_dialog_support/circle_urls.py smds/urls.py
            mkdir -p media/simple_data_export_uploads
            mkdir -p media/incoming_message_media
            mkdir -p media/outgoing_message_media
            python manage.py migrate
            python manage.py test
            cp simple_messaging_dialog_support/.pylintrc .
            pylint quicksilver
            pylint django_dialog_engine
            pylint simple_messaging
            pylint simple_messaging_dialog_support
            bandit -r .      
      - save_cache:
          key: venv-38-{{ .Branch }}-{{ checksum "/home/circleci/django/smds/simple_messaging_dialog_support/requirements.txt" }}
          paths:
            - "/home/circleci/venv"
      - store_test_results:
          path: test-results/
      - store_artifacts:
          path: test-reports/
          destination: python_app

  build-27:
    docker:
      - image: cimg/python:2.7
      - image: cimg/postgres:9.6.24-postgis
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: circle_test
          POSTGRES_PASSWORD: ""
    steps:
      - checkout
      - restore_cache:
          key: venv-2-{{ .Branch }}-{{ checksum "requirements.txt" }}
      - run:
          name: Build and validate
          command: |
            sudo apt-get update && sudo apt-get install -y gdal-bin
            virtualenv --copies /home/circleci/venv
            . /home/circleci/venv/bin/activate
            pip install -r requirements.txt --progress-bar off
            cd ..
            mv project simple_messaging_dialog_support
            mkdir django
            cd django
            django-admin.py startproject smds
            mv ../simple_messaging_dialog_support smds
            cd smds
            git clone https://github.com/audacious-software/Simple-Messaging-Django.git simple_messaging
            pip install -r simple_messaging/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Django-Dialog-Engine.git django_dialog_engine
            pip install -r django_dialog_engine/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Quicksilver-Django.git quicksilver
            pip install -r quicksilver/requirements.txt --progress-bar off
            git clone https://github.com/audacious-software/Simple-Data-Export-Django.git simple_data_export
            pip install -r simple_data_export/requirements.txt --progress-bar off
            cp simple_messaging_dialog_support/circle_settings.py smds/settings.py
            cp simple_messaging_dialog_support/circle_urls.py smds/urls.py
            mkdir -p media/simple_data_export_uploads
            mkdir -p media/incoming_message_media
            mkdir -p media/outgoing_message_media
            python manage.py migrate
            python manage.py test
            cp simple_messaging_dialog_support/.pylintrc .
            pylint quicksilver
            pylint django_dialog_engine
            pylint simple_messaging
            pylint simple_messaging_dialog_support
            bandit -r .      
      - save_cache:
          key: venv-27-{{ .Branch }}-{{ checksum "/home/circleci/django/smds/simple_messaging_dialog_support/requirements.txt" }}
          paths:
            - "/home/circleci/venv"
      - store_test_results:
          path: test-results/
      - store_artifacts:
          path: test-reports/
          destination: python_app
